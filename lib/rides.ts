export async function getRide(id: string) {
    // Implement your ride fetching logic here
    return {
        destination: string,
        seatsRemaining: number,
        meetingPoint: string,
        departureTimeMs: number
    }
}
